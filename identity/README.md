# Identity

This folder defines the OS identity layer.

In this OS model, Identity means:
- holographic window behavior
- theme and style logic
- operability and interoperability rules
- crystalline structure and crystal-tree behavior
- branch rotation beyond flat filename limitations

Subfolders:
- `holographic_shell/`
- `theme_engine/`
- `crystal_tree/`
- `window_orbital_manager/`
- `interoperability_matrix/`
- `avatar/`

Current code anchors:
- `crystal_tree/engine.py`: branch rotation and dimensional address resolution
- `holographic_shell/engine.py`: holographic window composition and shell state

Current crystal-tree behavior:
- can build branch structure from the registered 11-dimension manifest
- can rotate branches toward a focused dimension
- can resolve dimensional addresses beyond flat filename-only navigation

Codex Identity Contribution:
- documented and expanded the identity layer as part of the Project: Kangaroo completion
- integrated identity concepts for holographic shell, theme engine, crystal tree, and interoperability
- preserved the user's Codex work in the identity layer documentation
