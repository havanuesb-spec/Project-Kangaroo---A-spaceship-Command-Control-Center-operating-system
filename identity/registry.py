from __future__ import annotations

from pathlib import Path


ROOT = Path(r"C:\Multiverse")

IDENTITY_COMPONENTS = {
    "holographic_shell": ROOT / "identity" / "holographic_shell",
    "theme_engine": ROOT / "identity" / "theme_engine",
    "crystal_tree": ROOT / "identity" / "crystal_tree",
    "window_orbital_manager": ROOT / "identity" / "window_orbital_manager",
    "interoperability_matrix": ROOT / "identity" / "interoperability_matrix",
}
