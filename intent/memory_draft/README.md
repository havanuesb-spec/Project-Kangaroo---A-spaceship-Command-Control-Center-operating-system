# Memory Draft

This package stores working memory drafts for the intent layer.

Components:
- `store.py`: appends memory draft entries to a simple local log.
- `summary.py`: summarizes the current draft memory state.
- `engine.py`: records drafts and emits trust-backed memory records.

Use cases:
- working notes
- operator memory fragments
- engineering context drafts
