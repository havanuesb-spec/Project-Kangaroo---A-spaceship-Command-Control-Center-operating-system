# Connectors

This folder stores temporary cross-links between dimensions and concrete folders.

Subfolders:
- `active/`: connectors for dimensions that already have an implemented subsystem.
- `pending/`: placeholders for dimensions that still need a dedicated subsystem.
